(@main
  (@main 
    0 0
    rax <- rbx <= rcx
    rdx <- rsi < 5
    rdi <- rbp = 7

    //number on left side (must switch)
    r8 <- -3 < r9
    r10 <- 10 = r11
    r12 <- 0 <= r13

    //two numbers (optimize out comparison)
    r14 <- 1 < 3
    r15 <- 3 <= 1

    return 
  )
)